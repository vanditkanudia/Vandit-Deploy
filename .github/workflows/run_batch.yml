name: Run Batch Script

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}
  

jobs:
  run-batch:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run batch script
        run: .\run_batch.bat
        shell: pwsh
